"""Step7 Sub50：Step7成果物を研究者向けMarkdownレポートへ統合する。"""
from __future__ import annotations

from datetime import date
from pathlib import Path
import shutil
from typing import Any

import pandas as pd

from Step7_Utils.statistics import FLAG


ANALYSIS_NAMES = [
    "解析対象項目棚卸し", "データ品質診断", "欠損値解析", "重複データ確認", "カテゴリ一覧作成",
    "カテゴリ数集計", "年度別件数推移", "基本統計量一覧", "全カテゴリクロス集計", "カイ二乗検定",
    "Fisher正確確率検定", "CramersV", "標準化残差分析", "オッズ比解析", "相対危険度",
    "多重比較補正", "年度比較", "学校種比較", "男女比較", "学年比較", "発生場所比較",
    "発生時間帯比較", "単語頻度", "品詞集計", "TFIDF", "共起ネットワーク", "Ngram解析",
    "Jaccard係数", "WordCloud", "文長解析", "キーワード抽出", "類似文章検索", "Apriori",
    "FPGrowth", "AssociationRule", "クラスタリング", "PCA", "tSNE", "UMAP", "異常値検出",
    "希少事例抽出", "特徴量ランキング", "ヒートマップ", "MosaicPlot", "SankeyDiagram",
    "BubbleChart", "NetworkGraph", "重要因子ランキング", "研究テーマ候補ランキング", "総合レポート生成",
]

# 各解析を単独で読んだときに言える範囲を明示する。複数Subを横断した結論はここへ混ぜない。
SUB_DATA_FINDINGS = {
    1: "解析対象の列、型、欠損、一意値数を確認でき、各列に適した解析方法を判断できる。",
    2: "ID欠損・重複・カテゴリ不一致など、解析前に修正が必要な品質問題の有無が分かる。",
    3: "列別欠損率と欠損パターンが分かる。欠損理由や欠損による偏りまでは断定できない。",
    4: "完全一致およびID重複の候補が分かる。候補を自動的に削除すべきとは限らない。",
    5: "各列に実在するカテゴリ値を網羅的に確認でき、表記揺れや想定外カテゴリを探せる。",
    6: "列ごとのカテゴリ数と分布規模が分かり、多カテゴリ列や希少カテゴリ列を把握できる。",
    7: "年度ごとの事例件数の増減が分かるが、母集団規模を調整した発生率ではない。",
    8: "数値として扱える列の分布・代表値・ばらつきが分かる。",
    9: "各カテゴリにおける歯牙障害と非歯牙障害の実数・割合を比較できる。",
    10: "列全体と歯牙障害区分の統計的関連の有無が分かるが、どのカテゴリが寄与したかは単独では確定しない。",
    11: "カテゴリごとの歯牙障害区分との関連を、少数セルにも対応するFisher検定で確認できる。",
    12: "列全体と歯牙障害区分の関連の強さをCramér's Vで比較できる。因果効果ではない。",
    13: "期待値より歯牙障害が多い・少ないカテゴリと、その乖離の大きさが分かる。",
    14: "各カテゴリの歯牙障害オッズ比と95%信頼区間が分かる。交絡調整前の粗オッズ比である。",
    15: "各カテゴリの歯牙障害リスク比と95%信頼区間が分かる。母集団発生率の推定ではない。",
    16: "多数のカテゴリ比較後もBH法で有意性を保つ候補が分かる。効果の大きさは別途確認が必要。",
    17: "年度別に歯牙障害割合の違いを確認でき、経年的な偏りの候補が分かる。",
    18: "学校種別の歯牙障害件数・割合差が分かる。学齢や活動内容の影響は調整していない。",
    19: "男女別の歯牙障害件数・割合差が分かる。性別そのものの因果効果は示さない。",
    20: "学年別の歯牙障害件数・割合差が分かる。学校種との重なりに注意が必要。",
    21: "発生場所別の歯牙障害件数・割合差が分かる。活動場面との重なりに注意が必要。",
    22: "時刻・時間帯列がないため、時間帯別比較はデータから実施できないことが分かる。",
    23: "単語の総出現回数と事例出現率が分かり、自由記載で一般的な語を確認できる。",
    24: "形態素の品詞構成と事例出現率が分かり、テキスト全体の言語的構成を確認できる。",
    25: "歯牙障害と非歯牙障害で相対的に特徴的な語がTF-IDF差から分かる。",
    26: "同一事例内で一緒に現れやすい語の組合せが分かる。文脈や因果関係は示さない。",
    27: "自由記載で反復される2語・3語の連続表現が分かり、典型的な記述パターンを確認できる。",
    28: "Jaccard解析は既存Step4成果を利用すべきで、Step7で重複計算する必要がないことが分かる。",
    29: "WordCloudは定量比較への追加価値が乏しく、頻度表を優先すべきことが分かる。",
    30: "事例記述の文字数・形態素数・解析対象語数の分布と、極端に長短な記述を確認できる。",
    31: "歯牙障害で出現しやすい・しにくい語と、OR・BH補正後の確実性が分かる。",
    32: "類似文章検索は既存Step4成果を利用すべきで、Step7で重複計算する必要がないことが分かる。",
    33: "データ中で一定以上の支持度を持つカテゴリ組合せが分かる。頻出は因果や重要性を意味しない。",
    34: "FP-GrowthはAprioriと同じ目的の再計算となり、追加知見がないため非採用である。",
    35: "カテゴリ間の併存ルールについて支持度・信頼度・liftが分かる。時間順序や因果方向は示さない。",
    36: "カテゴリ構成の似た事例群と代表特徴が分かる。silhouetteが低い場合は明瞭な群と解釈しない。",
    37: "カテゴリ変動を要約する主成分と寄与率が分かる。主成分軸は直接的な臨床概念ではない。",
    38: "t-SNEはパラメータ依存性が強く定量解釈できないため、今回非採用としたことが分かる。",
    39: "UMAPは設定依存で追加価値が明確でないため、今回非採用としたことが分かる。",
    40: "希少カテゴリや極端な文長を持つ確認候補が分かる。異常だから除外するという判定ではない。",
    41: "5件以下のカテゴリを含む希少事例と該当理由が分かる。公表時の再識別リスク確認に使える。",
    42: "歯牙障害区分との相互情報量が大きい特徴量が分かる。予測的重要度や因果的重要度ではない。",
    43: "歯牙障害が期待値より多い・少ないカテゴリ上位を色と残差値で視覚的に確認できる。",
    44: "場合別2の構成比が歯牙障害群と非歯牙障害群でどう異なるかを面積で確認できる。",
    45: "カテゴリ関係を時間的な流れと誤読させるため、Sankey図を非採用としたことが分かる。",
    46: "割合差、オッズ比、件数を同時に見て、規模と関連の強さを持つカテゴリ候補を確認できる。",
    47: "主要語の共起構造と中心的な語群を視覚的に確認できる。ネットワークは因果構造ではない。",
    48: "複数の関連指標を統合した因子の探索的優先順位が分かる。交絡調整済み順位ではない。",
    49: "効果量・割合差・確実性・件数を統合した研究テーマ候補の優先順位が分かる。",
    50: "Step7全体の主要知見、成果物、限界、論文反映前の確認事項をまとめて確認できる。",
}

# 今回の「全体」データで実際に得られた結果。可能性の説明と観測結果を混同しないため別管理する。
SUB_RESULT_FINDINGS = {
    1: "18列・7,682件を確認し、ID候補、カテゴリ列、数値候補、自由記載列を特定した。",
    2: "ID欠損、ID重複候補、完全一致重複候補、入力元カテゴリ不一致はいずれも0件だった。",
    3: "完全データ行は0件だった。競技種目など用途限定列の欠損を含むため、解析ごとの欠損処理が必要である。",
    4: "完全一致重複候補と記号ID重複候補はいずれも0件で、自動削除対象は検出されなかった。",
    5: "解析列に実在するカテゴリ値を215件確認した。以後の比較はこの実在値を使用している。",
    6: "15列のカテゴリ数を集計し、多水準列と少数カテゴリを持つ列を確認した。",
    7: "19年度分の件数推移を作成でき、年度値の数値化不能は0件だった。",
    8: "実質的な数値列は2列で、主要データの多くがカテゴリ変数であることを確認した。",
    9: "歯牙障害対非歯牙障害について189カテゴリの件数・割合を比較できた。",
    10: "12列を検定し、学校種、場合別、通学方法、発生場所などに統計的関連を認めた。",
    11: "189カテゴリをFisher検定で評価し、カテゴリ単位の関連候補を抽出した。",
    12: "関連の強い列は場合別2、通学方法、場合別1、被災学校種、発生場所2だった。",
    13: "自転車、高校、道路、通学中などで歯牙障害側への大きな正の残差を確認した。",
    14: "自転車、道路、通学中などで歯牙障害の粗オッズが高い結果だった。",
    15: "カテゴリ別の粗リスク比と95%信頼区間を189カテゴリについて算出した。",
    16: "189比較中55件がBH補正後も5%水準で有意だった。",
    17: "19年度について歯牙障害割合を比較し、年度による構成差を評価できた。",
    18: "高校では歯牙障害が多く、小学校・保育区分では少ない方向の差がみられた。",
    19: "男女2カテゴリの比較が成立し、性別差は主要因子群より小さい関連だった。",
    20: "学年7カテゴリを比較し、学年による割合差の候補を確認した。",
    21: "発生場所29カテゴリを比較し、道路・学校外などで歯牙障害が多い方向を確認した。",
    22: "時刻または時間帯列が存在せず、推測変換もしないため解析不能と判定した。",
    23: "7,682事例から7,093種類の語を集計し、自由記載の頻出語と事例出現率を得た。",
    24: "382,640形態素を45品詞区分に集計し、テキストの品詞構成を確認した。",
    25: "2,508語について群間TF-IDF差を算出し、歯牙障害に特徴的な語を抽出した。",
    26: "最小5事例の条件で7,093ノード、43,440共起エッジを抽出した。",
    27: "最小3事例の条件で11,242件の2-gram・3-gramを抽出した。",
    28: "Step4にJaccard成果が19件あるため、重複計算せず既存成果利用とした。",
    29: "定量表より追加価値が乏しいためWordCloudを非採用とした。",
    30: "7,682事例すべてについて文字数・形態素数・解析対象語数を算出した。",
    31: "2,508語を比較し、BH補正後も有意な歯牙障害関連語を349語確認した。",
    32: "Step4に類似事例成果が6件あるため、重複計算せず既存成果利用とした。",
    33: "支持度1%以上で2,959件の頻出項目集合を抽出した。",
    34: "Aprioriと同じ結果を再計算するため、FP-Growthを非採用とした。",
    35: "信頼度20%以上かつlift 1.20以上の関連ルールを4,211件抽出した。",
    36: "k=7が選択されたがsilhouetteは0.145で、明瞭な自然クラスタとは言いにくい結果だった。",
    37: "10主成分の累積寄与率は60.40%で、カテゴリ変動の約6割を要約した。",
    38: "軸・距離を定量解釈できないためt-SNEを非採用とした。",
    39: "パラメータ依存性と追加価値を考慮してUMAPを非採用とした。",
    40: "異常・確認候補を194件抽出したが、いずれも自動除外していない。",
    41: "希少カテゴリを含む52事例を抽出し、自由記載を除いて確認可能にした。",
    42: "11列を評価し、相互情報量では場合別2、被災学校種、場合別1が上位だった。",
    43: "残差上位では自転車が+19.41、高校が+16.16、道路が+15.17だった。",
    44: "場合別2の上位8水準とその他について、両群の構成比の違いを可視化した。",
    45: "時間的流れでない関係を誤読させる可能性があるためSankey図を非採用とした。",
    46: "高校、道路、通学中、自転車などが規模と関連の両面で主要候補として示された。",
    47: "上位18語・27エッジの共起ネットワークを作成し、主要語群の結び付きを可視化した。",
    48: "安定性条件追加後の総合順位は、場合別2、通学方法、被災学校種、場合別1、発生場所2の順だった。",
    49: "高校、道路、通学中、自転車、小学校などを上位研究テーマ候補として選定した。",
    50: "全50解析を統合し、主要知見、非採用理由、限界、次段階の多変量解析課題を整理した。",
}

GROUP_FACTS = {
    "歯牙障害": {"n": 1583, "categories": 158, "words": 2640, "morphemes": 70325, "pos": 43,
                 "co_nodes": 2640, "co_edges": 6259, "ngrams": 1901, "itemsets": 3025,
                 "rules": 4521, "k": 4, "silhouette": 0.1986, "pca": 65.52,
                 "outliers": 30, "rare": 130, "graph_edges": 27},
    "歯牙障害以外": {"n": 6099, "categories": 212, "words": 6732, "morphemes": 312315, "pos": 45,
                    "co_nodes": 6732, "co_edges": 35828, "ngrams": 8894, "itemsets": 2916,
                    "rules": 4140, "k": 2, "silhouette": 0.1192, "pca": 59.72,
                    "outliers": 148, "rare": 57, "graph_edges": 30},
}


def _single_group_result(dataset_name: str, number: int) -> str:
    """単独群のResultへ、処理件数ではなく実際の観測内容を記載する。"""
    fact = GROUP_FACTS[dataset_name]
    comparison_unavailable = "歯牙障害と非歯牙障害の片方だけのデータなので、両群の差を調べるこの解析は実施できなかった。"
    descriptions = {
        1: f'{fact["n"]:,}件・18列を確認し、ID、カテゴリ、数値候補、自由記載の各列を特定した。',
        2: "ID欠損、ID重複候補、完全一致重複候補、入力元カテゴリ不一致はいずれも0件だった。",
        3: f'全項目が欠けていない事例は0/{fact["n"]:,}件だった。用途限定項目の欠損を含むため、解析ごとの扱いが必要である。',
        4: "完全一致重複候補と記号ID重複候補はいずれも0件だった。",
        5: f'{fact["categories"]}種類のカテゴリ値を確認し、実在する表記を一覧化した。',
        6: "15列についてカテゴリ数と分布を集計した。",
        7: "19年度分の件数推移を作成でき、年度の数値化不能は0件だった。",
        8: "数値として要約できる列は2列で、データの中心はカテゴリ項目だった。",
        22: "時刻・時間帯の列が存在しないため、推測による時間帯比較は行わなかった。",
        23: f'{fact["n"]:,}事例から{fact["words"]:,}種類の語を抽出し、総出現回数と事例出現率を集計した。',
        24: f'{fact["morphemes"]:,}形態素を{fact["pos"]}品詞区分に集計した。',
        25: comparison_unavailable,
        26: f'最小5事例の条件で{fact["co_nodes"]:,}語、{fact["co_edges"]:,}組の共起を抽出した。',
        27: f'最小3事例の条件で{fact["ngrams"]:,}件の2語・3語の連続表現を抽出した。',
        28: "Step4に既存のJaccard解析成果があるため、重複計算しなかった。",
        29: "定量的な頻度表より追加価値が小さいため、WordCloudを非採用とした。",
        30: f'{fact["n"]:,}事例すべてについて、文字数・形態素数・解析対象語数を算出した。',
        31: comparison_unavailable,
        32: "Step4に既存の類似事例検索成果があるため、重複計算しなかった。",
        33: f'支持度1%以上で{fact["itemsets"]:,}件の頻出カテゴリ組合せを抽出した。',
        34: "Aprioriと同じ目的の再計算となるため、FP-Growthを非採用とした。",
        35: f'信頼度20%以上かつlift 1.20以上の関連ルールを{fact["rules"]:,}件抽出した。',
        36: f'最も高い分離評価となったクラスタ数は{fact["k"]}、silhouetteは{fact["silhouette"]:.4f}で、明瞭な群分けとは言いにくい結果だった。',
        37: f'10主成分の累積寄与率は{fact["pca"]:.2f}%で、カテゴリ情報の約6割を要約した。',
        38: "配置が設定に強く左右されるため、t-SNEを非採用とした。",
        39: "設定依存性と追加価値を考慮し、UMAPを非採用とした。",
        40: f'希少カテゴリまたは極端な文長を持つ確認候補を{fact["outliers"]}件抽出したが、自動除外していない。',
        41: f'5件以下のカテゴリを含む希少事例を{fact["rare"]}件抽出し、自由記載を除いて一覧化した。',
        42: comparison_unavailable, 43: comparison_unavailable, 44: comparison_unavailable,
        45: "カテゴリ関係を時間的な流れと誤読させる可能性があるため、Sankey図を非採用とした。",
        46: comparison_unavailable,
        47: f'関連強度上位18語・{fact["graph_edges"]}エッジの共起ネットワークを作成した。',
        48: comparison_unavailable, 49: comparison_unavailable,
        50: f'{dataset_name}{fact["n"]:,}件について、実施可能な解析結果、非採用理由、成果物の所在を総合レポートへ整理した。',
    }
    if 9 <= number <= 21:
        return comparison_unavailable
    return descriptions[number]


def _read_optional(path: Path) -> pd.DataFrame | None:
    """先行成果物が未生成でも例外にせず、その事実をレポートへ残せるように読む。"""
    if not path.exists():
        return None
    return pd.read_csv(path, encoding="utf-8-sig")


def _format_number(value: Any, digits: int = 3) -> str:
    """表内の数値を過度な桁数にせず、人が確認しやすい文字列にする。"""
    try:
        return f"{float(value):.{digits}f}"
    except (TypeError, ValueError):
        return str(value)


def _write_sub_result_files(settings: dict[str, Any]) -> int:
    """全Subの成果物・抜粋・主要要約を、データセット別の一つのテキストへ統合する。"""
    output_dir: Path = settings["output_dir"]
    report_dir: Path = settings["report_dir"]
    old_result_dir = report_dir / "Sub別Result"
    if old_result_dir.exists():
        shutil.rmtree(old_result_dir)
    old_combined = report_dir / "Step7-01〜50_Result.txt"
    dataset_name = str(settings["dataset_name"])
    df = settings["df"]
    source_files = [
        path for folder_name in ["Table", "Summary", "CSV", "Figure"]
        for path in (output_dir / folder_name).glob("*") if path.is_file()
    ]
    factors = _read_optional(settings["summary_dir"] / "Step7-48_重要因子ランキング.csv")
    themes = _read_optional(settings["summary_dir"] / "Step7-49_研究テーマ候補ランキング.csv")
    if dataset_name == "全体":
        overview = [
            "・Step2確認済みデータ7,682件中、歯牙障害は1,583件、歯牙障害以外は6,099件。",
            "・完全一致重複候補および記号ID重複候補は0件。",
            "・歯牙障害との関連が強い主要因子は、学校種、場合別2、場合別1、通学方法、発生場所2。",
            "・特に高校、通学中、自転車、道路は歯牙障害が多い方向で関連した。",
            "・これらは相互に重なる可能性があり、独立した影響の確認には多変量解析が必要。",
        ]
    elif dataset_name == "歯牙障害":
        overview = [
            f"・歯牙障害{len(df):,}件を対象とした記述的・テキスト・パターン解析結果である。",
            "・非歯牙障害群を含まないため、群間検定、OR、関連因子ランキングは解析不能である。",
            "・単語頻度、共起、N-gram、クラスタリング、PCA、希少事例は歯牙障害群内で確認できる。",
            "・クラスタリングはk=4、共起図は上位18語・27エッジ、希少事例は130件だった。",
        ]
    else:
        overview = [
            f"・歯牙障害以外{len(df):,}件を対象とした記述的・テキスト・パターン解析結果である。",
            "・歯牙障害群を含まないため、群間検定、OR、関連因子ランキングは解析不能である。",
            "・単語頻度、共起、N-gram、クラスタリング、PCA、希少事例は非歯牙障害群内で確認できる。",
            "・クラスタリングはk=2、共起図は上位18語・30エッジ、希少事例は57件だった。",
        ]
    all_lines = [
        "SportDent Step7 Sub01〜50 統合Result", "=" * 72,
        f"データセット: {dataset_name}", f"対象件数: {len(df):,}件", "",
        "【結果から分かる要約】", "",
        *overview,
        "・関連・共起・クラスタは因果関係を意味せず、結果は仮説生成として扱う。", "",
    ]
    if factors is not None and not factors.empty:
        all_lines.append("重要因子上位5: " + "、".join(factors.head(5)["因子"].astype(str)))
    if themes is not None and not themes.empty:
        all_lines.extend(["", "推奨研究テーマ上位:"])
        for _, row in themes.head(10).iterrows():
            all_lines.append(f'・{row["研究テーマ候補"]}（OR={_format_number(row["オッズ比"], 2)}）')
    all_lines.extend(["", "=" * 72, "【Sub別結果】", ""])

    for number, analysis_name in enumerate(ANALYSIS_NAMES, start=1):
        prefix = f"Step7-{number:02d}"
        matched = sorted(path for path in source_files if path.name.startswith(prefix))
        text_lines = [
            f"Sub{number:02d} {analysis_name}", "-" * 60, "",
            "【このデータでわかること】",
            SUB_DATA_FINDINGS[number], "",
            "【この結果でわかること】",
            (SUB_RESULT_FINDINGS[number] if dataset_name == "全体"
             else _single_group_result(dataset_name, number)), "",
        ]
        if number == 50:
            text_lines.extend([
                "生成成果物:",
                "- Report/Step7-50_探索的解析総合レポート.md",
                "- Summary/Step7-50_成果物一覧.csv",
                "- Report/Step7-01〜50_Result.txt", "",
            ])
        elif not matched:
            text_lines.extend([
                "専用成果物: なし", "",
                "このデータセットでは解析条件が成立しなかった、または専用ファイルを生成しない解析です。",
                "詳細な理由はStep7_RunLog.txtを確認してください。", "",
            ])
        else:
            text_lines.extend([f"成果物数: {len(matched)}", ""])
            for path in matched:
                relative = path.relative_to(output_dir)
                text_lines.extend([f"■ {relative}", ""])
                if path.suffix.lower() == ".csv":
                    try:
                        data = pd.read_csv(path, encoding="utf-8-sig")
                        text_lines.append(f"行数: {len(data)} / 列数: {len(data.columns)}")
                        text_lines.append("列名: " + " / ".join(map(str, data.columns)))
                        text_lines.append("先頭10行:")
                        text_lines.append(data.head(10).to_csv(index=False, sep="\t", lineterminator="\n").rstrip())
                        if len(data) > 10:
                            text_lines.append(f"（残り{len(data) - 10}行は元CSVを参照）")
                    except Exception as exc:
                        text_lines.append(f"内容の読込に失敗: {type(exc).__name__}: {exc}")
                else:
                    text_lines.append("図ファイルです。画像またはSVG本体を参照してください。")
                text_lines.append("")
        all_lines.extend(text_lines)
    old_combined.write_text("\n".join(all_lines), encoding="utf-8")
    return 1


def _write_favorite_info(settings: dict[str, Any]) -> int:
    """Step7の観測結果から、論文利用候補と次段階の判断材料を研究者向けに整理する。"""
    if settings["dataset_name"] != "全体":
        return 0
    favorite_dir = Path(settings["output_root"]) / "FavariteInfo"
    favorite_dir.mkdir(parents=True, exist_ok=True)

    overview = """# Step7から研究利用できる重要情報

## この資料の位置付け

以下はStep7の出力を根拠にCodexが研究利用の観点から整理した提案です。実際の集計結果と、そこから考えられる研究上の見方を分けています。「ある特徴と歯牙障害が一緒に多くみられた」ことは分かりますが、その特徴が歯牙障害の原因だと確定する資料ではありません。

## はじめに知っておきたい用語

- オッズ比（OR）：ある条件で歯牙障害がどの程度多いかを比で表した値です。1より大きいと多い方向、1より小さいと少ない方向ですが、原因を示す値ではありません。
- 95%信頼区間（95%CI）：オッズ比の推定値にどの程度の幅があるかを示します。幅が狭いほど推定が比較的安定しています。
- 多変量解析：学校種や通学方法など、複数の条件を同時に考慮して関連を調べる方法です。
- 交絡：別の条件が重なり、本当の関連が分かりにくくなることです。
- BH補正：多数の項目を一度に調べたとき、偶然の「差あり」を減らす調整方法です。
- p値：観察された差が偶然でも起こり得るかを考えるための値です。小さいだけで重要とは限りません。
- 効果量：差や関連がどの程度大きいかを表す値です。
- 共起ネットワーク：同じ文章内に一緒に現れやすい言葉を線で結んだ図です。
- 再識別リスク：少数事例の特徴から、個人が推測される危険性です。

## 優先度A：通学関連の歯牙障害

集計結果：歯牙障害が多い方向のオッズ比は、自転車6.90（推定幅5.53–8.61）、道路3.50（2.95–4.14）、通学中3.49（2.94–4.15）、高校2.49（2.22–2.79）でした。

Codexの見方：これらは別々の4テーマというより、「高校生の自転車通学時に道路で生じる歯牙障害」という一つの状況を別の項目から見ている可能性があります。同じ事例に高校・自転車・通学中・道路が同時に当てはまることがあるため、それぞれが独立した原因だとは言えません。

利用案：学校種、通学中、自転車、道路、性別、年度を同時に考慮する統計解析を行い、ほかの条件をそろえても関連が残るかを確認します。「通学中かつ自転車」の組合せも、該当件数を確認してから検討します。

## 優先度A：学校段階による特徴差

集計結果：高校は歯牙障害が多い方向（オッズ比2.49）、小学校は少ない方向（オッズ比0.48）でした。少数セルの影響を抑えた関連項目の総合順位では、学校種は3位でした。

Codexの見方：学校種には年齢、活動内容、通学方法の違いも含まれます。「高校だから危険」と結論せず、これらの違いを同時に考慮しても関連が残るかを確認する必要があります。

## 優先度B：野球関連

集計結果：野球（軟式を含む）のオッズ比は1.99（推定幅1.73–2.29）で、歯牙障害群とそれ以外の群の割合には9.69ポイントの差がありました。

Codexの見方：通学とは別に、スポーツ中の予防を考えるテーマ候補です。事故が起きた流れ、顔へのボール・用具の接触、防具使用の有無を文章記録から追加確認できれば、具体的な予防策を考えやすくなります。

## 優先度B：自由記載に表れる歯牙障害固有語

集計結果：「前歯」は歯牙障害事例の45.55%に記載され、それ以外では0.48%でした。「歯」「口」「歯科」「補綴（失った歯などを人工物で補う治療）」「顔面」も歯牙障害側で多く、多数項目を調べた影響を調整した後も差が残りました。

Codexの見方：「前歯」と「歯」は別の言葉として扱う現在の方針が適切です。一方、「歯科」「補綴」は事故原因ではなく、診断や治療結果を直接表す可能性があります。歯牙障害を予測する材料に使うと、答えを先に教えるような不公平な分析になります。

## 補助的知見

- 似た事例を自動的にまとめる分析では7グループが選ばれましたが、グループの分かれ方を示す点数は0.145と低く、明確な7種類が存在するとは言えません。
- 多数の項目を少数のまとめ軸へ圧縮する分析では、10軸で元の情報の60.40%を説明しました。少数の軸だけでは全体を十分に表せません。
- 一緒に現れやすい語やカテゴリの分析は典型的な組合せの発見に役立ちますが、どちらが先か、どちらが原因かは分かりません。
- 希少事例・異常候補は自動除外せず、品質確認と再識別リスク確認に利用するのが適切です。

## 現段階で避けるべき表現

- 「自転車が歯牙障害を6.9倍発生させる」：ほかの条件を同時に考慮する前の比較値であり、原因による増加率ではありません。
- 「7つの事故タイプが存在する」：グループの分かれ方が不明瞭なため断定できません。
- 「共起ネットワークが受傷経路を示す」：語の同時出現であり、経路ではありません。
- 「p値が小さいほど臨床的に重要」：件数の影響を受けるため、効果量と割合差も必要です。
"""
    (favorite_dir / "01_研究利用できる重要情報.md").write_text(overview, encoding="utf-8")

    candidates = [
        [1, "高校生の自転車通学・道路上事故", "最優先", "高校オッズ比=2.49、自転車=6.90、道路=3.50、通学中=3.49", "複数条件を同時に考慮する解析", "各条件が同じ事例で重なる可能性"],
        [2, "学校段階と歯牙障害", "高", "関連項目3位、高校オッズ比=2.49、小学校=0.48", "年齢や活動等も同時に考慮する解析", "年齢・活動・通学方法の影響が重なる"],
        [3, "野球活動中の歯牙障害", "高", "野球オッズ比=1.99、割合差=9.69ポイント", "文章記録から事故の流れを分類", "競技参加者全体の人数が不明"],
        [4, "登校時と下校時の比較", "中", "登校オッズ比=3.61、下校=3.00", "通学事例だけに絞った比較", "どちらも『通学中』に含まれる"],
        [5, "歯牙障害記述の特徴語", "中", "前歯45.55%、歯28.05%、顔面30.89%", "事故の流れを示す語と診断語を分ける", "診断語が分析の答えを先に示す可能性"],
    ]
    pd.DataFrame(candidates, columns=["順位", "研究利用候補", "優先度", "根拠", "推奨する次解析", "主要な注意点"]).to_csv(
        favorite_dir / "02_論文利用候補.csv", index=False, encoding=settings["text_encoding"]
    )

    plan = """# 次段階の解析計画案

この資料は一般の方にも目的が伝わるよう、専門的な分析名には説明を添えています。

用語：クロス集計は条件の組合せごとに人数を数えた表、セルはその表の一つ一つの欄、モデルは複数条件をまとめて調べる計算式を意味します。

## 第一解析

- 調べたい結果：歯牙障害か、歯牙障害以外か
- 主に調べる条件：自転車、通学中、道路、高校
- 同時に考慮する候補：性別、年度、学校種、学年
- 出力：ほかの条件を考慮したオッズ比（歯牙障害の多さを示す比較値）、推定値の幅、偶然の差かを考えるための値
- 結果の安定性確認：欠損を一つの区分にした場合と、欠損を除いた場合を比較

## モデル設計前の確認

1. 高校・学年・自転車・通学中・道路のクロス集計とセル数を確認する。
2. 特定の組合せに片方の結果しかない状態があれば、少数データに対応した分析方法を検討する。
3. 学校種と学年を同時投入する必要性を確認する。
4. 条件の組合せによる違いは、事前に意味のある「通学中かつ自転車」などへ限定する。
5. 探索モデルと最終報告モデルを区別する。

## 論文への反映順

1. 研究方法へStep2入力、欠損処理、多数比較の調整、ほかの条件を考慮する前のオッズ比であることを記載。
2. 結果へ件数、割合の差、ほかの条件を考慮する前のオッズ比を先に記載。
3. 調整解析後にのみ独立関連について記載。
4. 論文の考察では制度・行動・環境の可能性として解釈し、原因だと断定しない。
"""
    (favorite_dir / "03_次段階解析計画案.md").write_text(plan, encoding="utf-8")

    checklist = [
        ["通学関連4因子のクロス集計", "未確認", "同一背景構造か確認"],
        ["説明項目同士の強い重なりを確認", "未実施", "複数条件を同時に調べる前に必要"],
        ["ほかの条件を考慮したオッズ比の算出", "未実施", "単純比較の値と比較"],
        ["野球事例の受傷機転分類", "未実施", "予防策へ接続"],
        ["人数が極端に少ない組合せの確認", "未実施", "分析結果の安定性"],
        ["Step6抄録への反映", "保留", "調整解析後に判断"],
    ]
    pd.DataFrame(checklist, columns=["確認項目", "現在の状態", "目的"]).to_csv(
        favorite_dir / "04_研究者確認チェックリスト.csv", index=False, encoding=settings["text_encoding"]
    )
    return 4


def run(settings: dict[str, Any]) -> None:
    """データ概要、主要知見、成果物一覧、注意事項を一つの報告書へまとめる。"""
    df = settings["df"]
    dataset_name = str(settings["dataset_name"])
    logger = settings["logger"]
    summary_dir: Path = settings["summary_dir"]
    output_dir: Path = settings["output_dir"]
    report_dir: Path = settings["report_dir"]

    tooth_count = int(df[FLAG].eq(1).sum()) if FLAG in df.columns else 0
    other_count = int(df[FLAG].eq(0).sum()) if FLAG in df.columns else 0
    missing_cells = int(df.isna().sum().sum())
    total_cells = int(df.shape[0] * df.shape[1])
    missing_rate = missing_cells / total_cells * 100 if total_cells else 0.0

    lines = [
        f"# SportDent Step7 探索的解析 総合レポート：{dataset_name}", "",
        f"- 生成日：{date.today().isoformat()}",
        f"- 対象件数：{len(df):,}件",
        f"- 歯牙障害：{tooth_count:,}件",
        f"- 歯牙障害以外：{other_count:,}件",
        f"- 列数：{len(df.columns)}列",
        f"- 全セル欠損率：{missing_rate:.2f}%", "",
        "## 解析目的", "",
        "Step2で抽出・目視確認されたカテゴリ別データを用い、歯牙障害の特徴と研究仮説を探索した。",
        "本レポートは探索結果の整理であり、因果推論や交絡調整済みの効果推定ではない。", "",
    ]

    factor_path = summary_dir / "Step7-48_重要因子ランキング.csv"
    theme_path = summary_dir / "Step7-49_研究テーマ候補ランキング.csv"
    factors = _read_optional(factor_path)
    themes = _read_optional(theme_path)
    lines.extend(["## 主要知見", ""])
    if factors is not None and not factors.empty:
        lines.extend(["### 重要因子 上位5", "", "| 順位 | 因子 | 総合重要度スコア | Cramér's V |", "|---:|---|---:|---:|"])
        for _, row in factors.head(5).iterrows():
            lines.append(f'| {int(row["順位"])} | {row["因子"]} | {_format_number(row["総合重要度スコア"], 2)} | {_format_number(row["CramersV（バイアス補正）"])} |')
        lines.extend(["", "総合重要度は複数の関連指標の順位を統合した探索的スコアであり、因果的重要度ではない。", ""])
    else:
        lines.extend(["重要因子ランキングは、このデータセットでは解析不能または未生成だった。", ""])

    if themes is not None and not themes.empty:
        lines.extend(["### 推奨研究テーマ 上位10", ""])
        for _, row in themes.head(10).iterrows():
            lines.append(
                f'{int(row["推奨順位"])}. {row["研究テーマ候補"]} '
                f'（OR={_format_number(row["オッズ比"], 2)}、割合差={_format_number(row["割合差（pt）"], 2)} pt）'
            )
        lines.extend(["", "テーマ候補は仮説生成用であり、変数間の重なりを考慮した多変量解析が次段階で必要となる。", ""])
    else:
        lines.extend(["研究テーマ候補ランキングは、このデータセットでは解析不能または未生成だった。", ""])

    result_file_count = _write_sub_result_files(settings)
    favorite_info_count = _write_favorite_info(settings)

    # 作成済みファイルを種類別に棚卸しし、レポートから成果物の所在を追えるようにする。
    inventory_rows = []
    lines.extend(["## 成果物一覧", ""])
    planned_files = {
        "Summary": "Step7-50_成果物一覧.csv",
        "Report": "Step7-50_探索的解析総合レポート.md",
    }
    for folder_name in ["Table", "Summary", "CSV", "Figure", "Report"]:
        folder = output_dir / folder_name
        file_names = sorted(str(p.relative_to(folder)) for p in folder.rglob("*") if p.is_file())
        planned_name = planned_files.get(folder_name)
        if planned_name and planned_name not in file_names:
            file_names.append(planned_name)
            file_names.sort()
        lines.append(f"### {folder_name}（{len(file_names)}ファイル）")
        lines.append("")
        for file_name in file_names:
            lines.append(f"- `{folder_name}/{file_name}`")
            inventory_rows.append({"データセット": dataset_name, "区分": folder_name, "ファイル名": file_name})
        if not file_names:
            lines.append("- なし")
        lines.append("")

    lines.extend([
        "## 解釈上の重要な注意", "",
        "- Step2のカテゴリ別抽出ファイルを統合したデータであり、元データベース全体を直接解析していない。",
        "- カテゴリ間に同一事例が含まれる可能性があるため、入力診断と重複候補を併せて確認する。",
        "- 多数の探索的比較を含む。BH補正済み結果を優先し、未補正p値のみで結論を決めない。",
        "- オッズ比、相関、共起、関連ルールは因果関係を意味しない。",
        "- 希少事例は自動除外していない。公表時は再識別リスクとセル秘匿を検討する。",
        "- Sankey、WordCloud、FP-Growth、t-SNE、UMAPは、重複・誤読・解釈上の理由から非採用として記録した。", "",
        "## 論文反映前の確認事項", "",
        "1. 上位テーマについて、年齢・学校種・活動場面などの交絡を調整した多変量解析を行う。",
        "2. 『高校・通学中・自転車・道路』のように意味的に重なる因子を同時投入する際は、多重共線性を確認する。",
        "3. 主要表の分母、欠損カテゴリの扱い、0.5補正の有無をMethodsに明記する。",
        "4. Step6のResults・Discussion・抄録へ反映する知見を研究者が最終選択する。", "",
    ])

    report_path = report_dir / "Step7-50_探索的解析総合レポート.md"
    report_path.write_text("\n".join(lines), encoding="utf-8")
    pd.DataFrame(inventory_rows).to_csv(
        summary_dir / "Step7-50_成果物一覧.csv", index=False, encoding=settings["text_encoding"]
    )
    logger.info("[%s] Sub50 / 総合レポート=%s / 統合Result=%d / FavariteInfo=%d / 棚卸し=%dファイル",
                dataset_name, report_path.name, result_file_count, favorite_info_count, len(inventory_rows))
