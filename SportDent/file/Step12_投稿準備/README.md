# SportDent Step12 投稿準備

`Step12_Main.py --force`は、Step10の確認済み原稿・図表を汎用の投稿準備パッケージへ整理します。Step11の旧単一自動分類、事故原文、内部IDは複製しません。既存パッケージがある場合は`--force`なしで停止します。

全工程の再生成後に`Validate_Step8_12.py`を実行すると、主要数値、Step10→12の複製、Step11の盲検標本、小集計、非公開権限、内部ID・原文漏洩、入力・コードハッシュを読み取り専用で確認します。
